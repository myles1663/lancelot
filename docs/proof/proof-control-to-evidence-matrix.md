# Proof of Governed Execution Control To Evidence Matrix

Status: fixed for `LDD-005 Proof of Governed Execution`.

| Claim | Case | Runtime path | Authority evidence | Receipt evidence | Operator/API evidence | Artifact | Limitation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Mutating UAB action without authority is denied before execution. | `missing-grant-denial` | Real `UABProvider` with `_rpc_call` spy. | No `UABAuthorityGrant`; provider returns `missing_authority_grant`; `_rpc_call` count remains zero. | Canonical denial receipt when `uabReceiptContext` is supplied; otherwise `local_denial_event_only`. | Receipt appears in proof bundle and operator visibility evidence, or direct `ReceiptService` caveat is recorded. | `proof-negative-cases.json`, `proof-receipt-bundle.json` | Live desktop target is not required. |
| Scoped authority grant permits one controlled action. | `valid-grant-execution` | Real `UABProvider` with deterministic `_rpc_call` success. | Signed deterministic fixture grant validates for action, app, PID, selector, risk, workflow, and run. | Canonical success receipt links to grant ID, workflow ID, run ID, app, action, selector scope, outcome, and grant policy version through the grant artifact. | Receipt and action result are included in packet evidence. | `proof-valid-grant-action.json`, `proof-receipt-bundle.json` | Proof key is a non-secret fixture key. |
| Hostile grants do not authorize mutation. | `hostile-grants` | Real `UABProvider`; `_rpc_call` is not invoked for denied hostile cases except the first valid use in the replay pair. | Tampered action, expired grant, wrong PID, wrong selector, unknown UAB risk, replayed nonce, and missing signature all produce explicit denial reasons. | Each denial records `canonical_denial_receipt` or `local_denial_event_only`; `receipt_created=true` only for canonical receipts. | Denial artifacts are included in packet evidence. | `proof-hostile-grant-cases.json`, `proof-negative-cases.json` | Replay claim is same-provider-instance only. |
| Replay of the same grant nonce is rejected on the same provider instance. | `hostile-grants/replayed-nonce` | One `UABProvider` instance executes first valid action, then receives the reused grant. | First use succeeds; second use is denied before `_rpc_call` because nonce was already seen. | Success receipt for first use; denial receipt for second use when context is supplied. | Receipt chain documents the replay scope limitation. | `proof-hostile-grant-cases.json`, `proof-receipt-chain.md` | Durable cross-process replay protection is not claimed. |
| UAB governance applies to data exposure as well as mutation. | `sensitive-reads` | Real `UABProvider` read paths with `_rpc_call` fake/spy. | Safe read is allowed without grant; sensitive read without grant is denied or escalated; sensitive read with valid grant is allowed. | Canonical success or denial receipts exist when receipt context is supplied. | Sensitive-read evidence appears in packet artifacts. | `proof-sensitive-read-cases.json`, `proof-receipt-bundle.json` | Optional live desktop sensitive-read evidence may be waived; controlled grant-backed sensitive read may not be waived. |
| A successful authorization can still produce a canonical failure receipt when the daemon fails. | `controlled-rpc-failure` | Real `UABProvider` authorizes the grant; deterministic `_rpc_call` returns failure. | Valid grant is accepted; `_rpc_call` is invoked exactly once; action is not reported successful. | Canonical failed receipt includes explicit error reason. | Failure receipt is included in receipt bundle and chain. | `proof-failure-case.json`, `proof-receipt-bundle.json`, `proof-receipt-chain.md` | No live desktop failure is required. |
| Receipt chain reconstructs the governed proof run. | `receipt-reconstruction` | `ReceiptService` persists canonical UAB receipts from the provider path. | Receipts link to relevant grants and case IDs. | Machine-readable bundle and human-readable chain show denied, successful, and failed outcomes. | Operator visibility evidence references the same receipt IDs where API auth is exercised or direct service lookup is documented. | `proof-receipt-bundle.json`, `proof-receipt-chain.md` | Local UAB audit is not canonical proof. |
| Runtime/operator evidence is collected without adding War Room scope. | `operator-visibility` | Served runtime or TestClient/session smoke for health/readiness/War Room route. | Proof run references deterministic workflow and run IDs. | Receipt visibility is shown through authenticated API/TestClient/session evidence, or direct `ReceiptService` lookup caveat. | HTTP statuses and visibility path are recorded. | `proof-runtime-smoke.json`, `proof-war-room-evidence.md` | No new UI panel or auth behavior is implemented. |
| Final packet makes only proven claims. | `final-packet` | One-command proof runner emits all required artifacts and packet zip. | Manifest records real `UABProvider` path and `_rpc_call` fake/spy mode. | Packet contains receipt bundle, chain, runtime evidence, limitations, and demo script. | Packet zip integrity validates with `python -m zipfile -t`. | `proof-of-governed-execution-packet.zip` | Broad hardening, release readiness, live desktop execution, HIVE, federation, multi-tenancy, and repo-wide coverage are not claimed. |
