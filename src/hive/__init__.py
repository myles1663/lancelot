"""
HIVE Agent Mesh — ephemeral sub-agent architecture for Lancelot.

Decomposes complex tasks into governed sub-agents that execute via UAB
under full operator pause/kill/modify control.

Feature-gated: FEATURE_HIVE (default: false)
"""
